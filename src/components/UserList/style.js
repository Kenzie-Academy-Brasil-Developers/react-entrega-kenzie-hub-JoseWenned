import { styled } from "styled-components";

export const ContainerUserList = styled.ul`
    display: flex;
    flex-direction: column;
    justify-content: center;

    border-top: 2px solid var(--color-gray-02);
    border-bottom: 2px solid var(--color-gray-02);
`
    